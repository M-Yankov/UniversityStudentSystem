@using UniversityStudentSystem.Common;

@model DateTime?

@(Html.Kendo().DateTimePickerFor(m => m).Culture(System.Threading.Thread.CurrentThread.CurrentCulture.Name).Format(WebConstants.WebDateFormat).TimeFormat("HH:mm"))